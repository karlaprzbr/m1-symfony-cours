{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row">
        <div class="col-6">
    <h1>Category</h1>
        </div>
        <div class="col-6 d-flex justify-content-end">
            <a href="{{ path('category_edit', {'id': category.id}) }}" class="btn btn-secondary h-75 shadow mx-2">Edit</a>
            {{ include('category/_delete_form.html.twig') }}
        </div>
    </div>
    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_index') }}">back to list</a>
</div>
{% endblock %}
